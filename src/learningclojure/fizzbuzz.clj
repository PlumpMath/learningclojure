
;
;(defn fizzbuzz [max]
;  "Prints fiz if a number is divisible by 3, buzz if divisible by 5 and fizbuzz if divisible by 3 and 5"
;  (loop [current 0, m max]
;    (if () ;current != max then println
;      ))))
